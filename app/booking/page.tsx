import BookingForm from "@/components/BookingForm";
import { metadataForPath } from "@/lib/seo-config";
import { loadSeoState } from "@/lib/seo-store";
export async function generateMetadata(){const seo=await loadSeoState();return metadataForPath("/booking",seo.pages)}
export default async function BookingPage({searchParams}:{searchParams:Promise<{package?:string}>}){const query=await searchParams;return <><section className="inner-hero inner-hero--booking"><div className="page-container inner-hero-grid"><div><span className="section-label">PLAN YOUR JOURNEY</span><h1>บอกแค่วันเดินทาง<br/><em>เราเริ่มช่วยต่อได้เลย</em></h1></div><p>ถ้ายังไม่แน่ใจว่าจะเลือกแพ็กเกจไหน ไม่เป็นไร ทีมจะช่วยดูจำนวนวัน เส้นทาง โรงแรม และเที่ยวบินให้เหมาะกับทริป</p></div></section><section className="section booking-page-section"><div className="page-container booking-page-grid"><div className="booking-how"><span className="section-label">HOW IT WORKS</span><h2>จากข้อมูลสั้น ๆ<br/>สู่ใบเสนอราคา</h2><div className="booking-step"><span>01</span><div><strong>ส่งวันและจำนวนคน</strong><p>พร้อมช่องทางติดต่อที่สะดวก</p></div></div><div className="booking-step"><span>02</span><div><strong>ทีมช่วยจัดทางเลือก</strong><p>แพ็กเกจ เที่ยวบิน โรงแรม และกิจกรรม</p></div></div><div className="booking-step"><span>03</span><div><strong>รับใบเสนอราคา</strong><p>ราคาตามวันเดินทางจริงและเงื่อนไขที่ชัดเจน</p></div></div></div><div className="booking-form-card"><BookingForm defaultPackage={query.package??""}/></div></div></section></>}
